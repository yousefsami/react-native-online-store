import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: '#eee',
  },
  rootScroll: {
    paddingBottom: 15,
    alignItems: 'center',
    alignSelf: 'center',
  },
});
